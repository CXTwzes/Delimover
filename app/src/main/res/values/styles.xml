<resources>

    <!-- Base application theme. -->

    <!-- The theme of the four buttons upside in the MY interface -->
    <style name="ButtonTheme">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1.0</item>
        <item name="android:textColor">#000000</item>
        <item name="android:background">#FF7800</item>
    </style>
    <style parent="Theme.AppCompat.Light.DarkActionBar" name="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style parent="AppTheme" name="Theme.Drawer.ArrowAnimation">
        <!-- 箭头 -->
        <item name="drawerArrowStyle">@style/AppTheme.DrawerArrowToggle</item>
    </style>
    <!-- 箭头颜色 -->
    <style parent="Base.Widget.AppCompat.DrawerArrowToggle" name="AppTheme.DrawerArrowToggle">
        <item name="color">@android:color/white</item>
    </style>
</resources>
